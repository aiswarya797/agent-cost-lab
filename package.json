{
  "name": "agent-cost-lab",
  "version": "0.1.0",
  "description": "Local cost-hygiene checks for agent-oriented projects.",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "agent-cost-lab": "dist/cli.js"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "npm run build && node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
